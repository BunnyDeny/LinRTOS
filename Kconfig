mainmenu "LinRTOS Configuration"

# Architecture / CPU / Compiler selection
source "arch/Kconfig"

# Components (3rd-party libraries / optional features)
source "Components/Kconfig"

# Introduce src subdirectory Kconfig
source "src/Kconfig"

# Example test cases
source "tests/Kconfig"
